# 技术美术优化概览 — 《三国杀》网页版美术/打击感升级

> 方向：**视觉质感与打击感优先**；平台：**桌面 + 移动都要**（所有特效走 GPU 合成，移动端预算可控）。

## 一、关键诊断（改动前的瓶颈）
1. `#app` 每帧 `innerHTML` 整块重建，每次状态变化都重算布局 + 重绘所有 `box-shadow`（模糊运算昂贵）。
2. 回合光效 `panelGlow` 直接**动 `box-shadow`** → 每帧重绘；且因 DOM 反复重建，动画被反复重启（既卡又闪）。
3. 3 处 `backdrop-filter: blur()` 在频繁重绘的背景下开销大。
4. 纯渐变太「平」，**完全没有打击反馈**：挨打、治疗、放技能都没有屏幕/局部反馈。

## 二、本次落地内容

### 架构（最重要的一条）
- 新建独立 `#vfx-layer` 覆盖层（`index.html`），所有瞬时特效由 `vfx.js` 在事件点**召唤、自动销毁**，**绝不进入游戏 DOM** —— 因此不会被整块重建清掉、也不会重复触发入场动画。

### 新增 / 修改文件
| 文件 | 改动 |
|------|------|
| `vfx.js`（新增） | 特效引擎：`VFX.fire(type, playerId)`，支持 伤害环+火花+全屏红闪+震屏 / 治疗绿脉冲 / 技能金光爆发；仅用 `transform`/`opacity`（GPU 合成），短生命周期自清理。 |
| `index.html` | 加入 `#vfx-layer`；在 `game.js` 后引入 `vfx.js`。 |
| `game.css` | ① 全局 SVG 颗粒噪点质感（body 背景层，`overlay` 混合，静态绘制≈零成本）；② hero 面板加深景深 + **GPU 回合光晕**替代动 box-shadow；③ 卡牌金属内边斜边 + 悬停「箔面流光」；④ 全套 VFX keyframes（伤害/治疗/技能/全屏闪/震屏）；⑤ 三处 `backdrop-filter` 模糊半径下调（4→3 / 3→2 / 2→1.5px）以保移动端预算。 |
| `game.js` | ① `renderHeroPanel` 给面板加 `id="hero-{id}"` 供特效定位；② 新增 `triggerVFX()` 方法；③ `dealDamage` 末尾挂伤害+震屏（全游戏唯一伤害漏斗，一处覆盖所有伤害）；④ `resolveTao` 挂治疗；⑤ 奸雄/天妒/雷击/火攻 四处技能点挂技能光爆。 |

## 三、性能预算对照（桌面/移动双达标）
- 所有 VFX 仅 `transform`/`opacity` → 合成器线程，不动主线程重绘。
- 颗粒质感是 body 静态背景层，仅在视口变化时重绘一次。
- 震屏带 220ms 节流，避免多段伤害叠加；节点 `animationend` 即销毁，无图层堆积。
- `backdrop-filter` 已下调，移动端模糊开销可控。

## 四、如何预览
本地静态服务已启动：打开 `http://localhost:8123/index.html`，开局后：
- 出【杀】/受到任何伤害 → 受击者面板爆出红色冲击环+火花、全屏红闪、整屏轻微震动。
- 使用【桃】回血 → 绿色脉冲光晕。
- 触发 奸雄/天妒/雷击/火攻 等技能 → 金色光爆。
- 鼠标悬停手牌 → 卡面出现对角「箔面流光」；当前行动者面板有柔和外发光呼吸。

## 五、可继续扩展（按需）
- 技能光爆目前接在 4 个代表技能；可批量接到更多 `addLog(...,'skill')` 点（建议集中钩子避免散落）。
- 出牌/摸牌的卡牌飞入动画需配合渲染层改造（避免被 innerHTML 重建重启），可作为下一步。
- 若后续引入真实美术资源（立绘/卡图），建议走纹理图集 + 压缩规范（BC7/ASTC），我可按项目规范出资源管线文档。
