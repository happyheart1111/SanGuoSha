# VFX 特效系统优化 — 技术美术工作总结

## 概述

为三国杀网页版引入了完整的 VFX（视觉效果）特效引擎，作为独立于游戏 `innerHTML` 重建周期的持久化特效层，提供伤害飘字、屏幕震动、技能光效、卡牌飞行、回合过渡等视觉反馈。

## 新增文件

- **`src/game/vfx.js`** — VFX 引擎核心模块，提供以下 API：
  - `VFX.damageEffect(player, amount)` — 伤害飘字 + 粒子爆发 + 屏幕震动 + 目标闪烁
  - `VFX.healEffect(player, amount)` — 治疗飘字 + 绿色粒子
  - `VFX.skillActivate(player)` — 技能激活光环扩散 + 金色粒子
  - `VFX.turnBanner(heroName, isHuman)` — 回合过渡横幅动画
  - `VFX.deathEffect(player)` — 死亡消散灰色粒子 + 暗色扩散环
  - `VFX.cardFly(from, to)` — 卡牌飞行轨迹动画
  - `VFX.screenShake(intensity)` — 屏幕震动
  - `VFX.particleBurst(x, y, color, count)` — 粒子爆发

## 修改文件

| 文件 | 修改内容 |
|------|---------|
| `game.css` | 新增 ~200 行 VFX 动画 CSS（飘字、震动、粒子、光环、横幅、死亡消散等） |
| `index.html` | 引入 `vfx.js` 脚本 |
| `src/game/01-core.js` | 三个 init 入口添加 `VFX.init()` + `VFX.clear()` |
| `src/game/02-turn.js` | 回合过渡横幅、治疗特效、酒技能特效、卡牌飞行、武圣技能特效、青囊AI路径 |
| `src/game/04-skills.js` | 青囊/好施/结姻/仁德/制衡/苦肉/雷击/奸雄/天妒 等技能 VFX 触发 |
| `src/game/05-combat.js` | 死亡消散特效、濒死自救治疗特效 |
| `src/game/08-ui.js` | `dealDamage` 伤害飘字 + 震动 + 粒子 |

## 架构设计

```
┌─────────────────────────────────────────┐
│           game-container (innerHTML 重建)  │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  │
│  │ player  │  │ player  │  │ player  │  │
│  │ panel   │  │ panel   │  │ panel   │  │
│  └─────────┘  └─────────┘  └─────────┘  │
│        ↑ 位置查询 (getBoundingClientRect)  │
├─────────────────────────────────────────┤
│         #vfx-layer (持久化，不随重建销毁)    │
│  ┌──────┐  ┌──────┐  ┌──────────────┐   │
│  │飘字  │  │粒子  │  │光环/横幅/震动 │   │
│  └──────┘  └──────┘  └──────────────┘   │
│  pointer-events:none / z-index:500      │
└─────────────────────────────────────────┘
```

**关键设计决策**：VFX 层是 `position:fixed` 的独立 DOM，挂在 `document.body` 上，完全不受游戏 `_doRender()` 的 `app.innerHTML = html` 重建影响。所有特效通过 `getBoundingClientRect()` 查询目标位置后生成临时 DOM 元素，动画结束后自动清理。

## VFX 触发点覆盖

| 事件 | VFX 效果 |
|------|---------|
| 受到伤害 | 红色飘字 `-1` + 红色粒子 + 屏幕震动 + 目标红色闪烁 |
| 使用桃/治疗 | 绿色飘字 `+1` + 绿色粒子 |
| 技能激活 | 金色光环扩散 + 金色粒子爆发 |
| 回合开始 | 金色横幅 `武将名 的回合` |
| 阵亡 | 灰色粒子爆发 + 暗色扩散环 |
| 出牌 | 卡牌飞行轨迹（从出牌者到目标） |
| 雷击命中 | 紫色 `⚡` 飘字 + 伤害特效 |
| 苦肉 | 技能光效 + 伤害特效 |
| 武圣/急救 | 技能光效 + 卡牌飞行 |

## 兼容性

- 纯 CSS + JS 实现，零依赖
- 所有 VFX 调用使用 `typeof VFX !== 'undefined'` 守卫，确保即使 VFX 加载失败也不影响游戏逻辑
- 响应式设计：VFX 层覆盖全屏，在移动端同样生效
- 不破坏现有 PvP/斗地主/身份局任何游戏模式

## 后续优化建议

1. **卡牌翻面动画** — 当前 innerHTML 重建模式限制，建议未来迁移到虚拟 DOM 后实现
2. **武将头像美术升级** — 当前为文字+渐变，可用 AI 生成武将立绘
3. **音效系统** — 配合 VFX 添加伤害/治疗/技能音效
4. **打击感增强** — 伤害时添加短暂的时间停滞（hitstop）效果
5. **特效配置面板** — 允许玩家在设置中开关 VFX 特效（性能优化）
