# 2026-08-09 工作日志

## 修复三张牌的 bug
1. **决斗回合挂起**：决斗中打不出杀的一方受伤害后，原代码 `_resumeSourcePlay(challenger)` 恢复的是"上一个出杀者"，导致 AI 发起决斗→玩家反击→AI 无杀时 AI 回合永久挂起（人类恢复分支为空操作）。修复：3 处（03-cards.js humanRespondJuedou/humanRespondJuedouSecond、ai.js aiRespondJuedou）统一改为恢复 `players[currentPlayerIdx]`（当前出牌方）；02-turn.js resolveAutoPlayPending 的 juedou case 同步修正。
2. **过河拆桥/顺手牵羊盲选卡死**：盲选模式（出牌者是人类）UI 只有手牌按钮，目标无手牌只有装备/判定区时无法选择→卡死。修复：08-ui.js 盲选按钮区补装备（slotNames）+判定区按钮，复用 humanGuoheDiscardEquip/Judge、humanShunshouStealEquip/Judge。
3. **顺手牵羊判定区牌处理错误**：原代码把判定区原卡 push 进弃牌堆再生成无 ownerId 副本。修复：直接 splice 移入手牌并设 ownerId。
4. **顺带**：AI 顺手牵羊加距离限制（calcDistance≤1）；新增统一 `cancelWait()` 替换所有内联取消按钮，防止取消导致 AI 回合挂起。

## 新增 9 名武将（heroes.js + game.css 头像 + 09-functions.js 图鉴色）
- **诸葛亮**(蜀3)：观星（准备阶段看牌堆顶X张，选任意张放底，02-turn startCurrentTurn 前触发，AI 随机放一半）+ 空城（无手牌不可为杀/决斗目标，getValidTargets + AI 目标过滤）
- **赵云**(蜀4)：龙胆（杀闪互换：出牌闪当杀 effectiveType、响应杀时杀当闪、决斗/AOE 闪当杀；人类+AI 全路径）
- **黄忠**(蜀4)：烈弓（resolveSha 里条件判断→_dealShaDamage 直接伤害）
- **马超**(蜀4)：马术（calcDistance -1）+ 铁骑（出杀判定红色不可响应，判定可被鬼才改）
- **夏侯惇**(魏4)：刚烈（dealDamage 触发，判定♠/♥，AI 来源受1伤，人类来源弹窗弃2手牌/受1伤）
- **曹仁**(魏4)：据守（出牌阶段限一次摸3张结束出牌，人类按钮+AI hp≤2 时用）
- **司马懿**(魏3)：反馈（dealDamage 触发获得来源一张牌）+ 鬼才（maybeGuicai 判定替换钩子，挂在延时锦囊/铁骑/刚烈判定，人类弹窗 AI 50%替换）
- **界徐盛**(吴4)：破军（resolveSha 出杀后弃目标一张牌，无手牌则伤害+1，人类盲选 UI 复用，AI 自动优先弃装备）
- **谋黄忠**(蜀4)：烈弓（谋）（useCardOnTarget 记录花色，出杀亮牌堆顶X张匹配花色加伤害且不可响应）

## 关键重构
- resolveSha 拆分为 resolveSha(谋烈弓/破军) → _beginShaResponse(烈弓/铁骑) → _beginShaResponseCore(闪响应)，新增 _dealShaDamage 保证奸雄等受击技能可取伤害牌
- resolveJudgePhase 重构为 _processJudgeCards 递归（支持鬼才回调）
- resolveAutoPlayPending 补齐 6 个新等待类型（poujun_discard/guanxing/guicai/ganglian_source/fankui_source），托管不卡死

## 测试
- 单元测试 9 项全过（决斗恢复×2、鬼才、刚烈）
- 8人局全托管 18 秒推进 10 轮自然结束，无死锁
- 已同步 dist/，语法检查通过

## 出牌目标选择交互改造（场上点选 + 灰色不可选）
- 需求：出牌时可选择玩家为对象，无法选择的玩家显示为灰色。
- 改造：`showTargetSelection` 不再用 targetOverlay 弹窗，改为设置 `waitingForTarget = { type: 'target_select', card, targets, callback }`，直接在场上玩家面板点选。
- 新增：`pickTarget(pid)`（点击可选目标执行回调）、`cancelTargetSelect()`（取消出牌）、`getTargetSelectState(player)`（返回 'selectable'/'blocked'）。
- `renderHeroPanel` 依据状态加 class：`target-selectable`（金色高亮+呼吸动画+scale 放大+pickTarget 点击）或 `target-blocked`（grayscale+opacity0.45+not-allowed 置灰）。出牌者自己/死亡玩家/非法目标一律置灰。
- action-bar 新增 target_select 提示文案 + "取消出牌"按钮。
- 清理残留：`humanEndPlayPhase` 与 `nextPlayer` 开头清除 target_select 状态。
- CSS：game.css 新增 .target-selectable/.target-blocked/@keyframes targetGlow。
- 规则核对：getValidTargets 已符合标准三国杀出牌目标规则（杀=距离内1人、决斗=1人无距离、过河拆桥=有牌1人无距离、顺手牵羊=距离1内有牌、火攻=有手牌、乐/兵粮=判定区<3、桃/酒=自己、南蛮/万箭/桃园/五谷/无中/闪电=无目标直接使用）。
- 验证：18 项单测全过（状态判定/点击回调/不可选拦截/取消/残留清理/HTML class 渲染），dist 已同步。

## 规则核对 + bug 修复 + 部署（下午）
- 搜索核对标准规则：杀每回合1张（咆哮/连弩突破）、乐非♥跳出牌、兵粮非♣跳摸牌、闪电♠2-9受3伤、铁索传导属性伤害、身份局胜负等。
- **修复 humanConfirmDiscard 崩溃**：`if (wt.qinyinPlayer)` 引用未定义变量 wt（解构时未声明）→ 人类玩家弃牌确认时必崩、回合无法推进。改为从 waitingForTarget 解构 qinyinPlayer。
- **修复身份局内奸胜误判**：主公死亡且反贼已全灭时（内奸单挑击杀主公）原判反贼胜。调整 checkGameOver 判断顺序：先判反贼内奸全灭→主公胜；主公死→反贼存活则反贼胜否则内奸胜。
- **实现铁索连环传导**：dealDamage 加第5参 damageType（'fire'/'thunder'），属性伤害 + target.linked + !_chainGuard 时传导给其他横置角色。雷击/火攻/业炎/闪电/神周瑜AI 业炎已标记属性。雷击改为走 dealDamage（击杀者=张角，可触发卖血技/传导）。
- 部署：CloudStudio 静态托管，分享链接 https://887b9081c457419096d2d307bd03a077.bj10.agentos-app.net（部署 dist/ 目录）。
- 验证：回归测试 12 项全过（弃牌确认/内奸胜×2/铁索传导×2/雷击传导）；身份局 8 人托管 20s 推进 9 轮、斗地主 4 轮，无死锁。
- 已知未实现（非崩溃 bug）：无懈可击、贾诩完杀、五谷丰登选牌顺序、部分武器特效（青龙/贯石/方天）、华佗急救"回合外"限制、PvP 谋黄忠花色同步。

## AI 出牌卡死排查 + 破军取消修复（傍晚）
- 用户报告"AI 出完牌后卡死"。排查：Node 模拟人类响应的完整对局（含随机决策模糊测试 5 局、托管模式 20 秒、身份局/新武将局/斗地主）均正常流转，未复现逻辑层卡死。aiPlayCards 各分支续链均完整（装备/无中/AOE/桃园/五谷/桃/乐/兵粮/闪电/拆桥/顺牵/决斗/酒/火攻/铁索/杀/据守 全部 setTimeout 续链或 endPlayPhase）。
- **修复破军取消 bug**：poujun_discard 状态下点"取消"原走 cancelWait 只清 waitingForTarget，导致杀被弃但未结算（手牌已 discardCard）。新增 `cancelPoujun()`：放弃发动破军 → 调 `_beginShaResponse(source,target,card,0)` 继续正常杀结算；盲选 UI 破军取消按钮改为"放弃破军"。
- 已重新部署（同沙箱，链接不变）。若仍卡，需用户提供具体场景（哪张牌/哪个武将/托管与否）。
