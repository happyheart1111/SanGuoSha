# 2026-08-08 工作日志

## VFX 特效系统搭建
- 创建 `src/game/vfx.js` — 独立于 innerHTML 重建的持久化 VFX 引擎
- VFX 层挂在 `document.body` 上的 `#vfx-layer`（position:fixed, z-index:500, pointer-events:none）
- 提供 API：damageEffect / healEffect / skillActivate / turnBanner / deathEffect / cardFly / particleBurst / screenShake
- 在 game.css 新增 ~200 行 VFX 动画 CSS
- 在 dealDamage / resolveTao / killPlayer / startCurrentTurn / useCardOnTarget / doLeiji / 各技能触发点接入 VFX
- 所有 VFX 调用用 `typeof VFX !== 'undefined'` 守卫，不影响游戏逻辑
- 三个 init 入口（init / initOld / initPvP）均添加 VFX.init() + VFX.clear()

## 项目部署（首次上线）
- htmlcode.fun（/HTML Deploy skill）当前部署服务暂停（402 Payment required，官方指南页显示 Deployment Paused），不可用
- 改用 CloudStudio 静态站点部署，线上地址：https://887b9081c457419096d2d307bd03a077.bj10.agentos-app.net
- 新增 `.workbuddy/scripts/bundle_standalone.py`：把多文件项目打包为单文件 `sanguosha-standalone.html`（内联 game.css + 18 个 src/*.js，保留 peerjs CDN 外链），产物 328KB
- 部署目录 `dist/`（index.html + game.css + src/ 共 20 文件，328KB），管理入口：设置-数据管理-我发布的应用
