# 座位呈现优化（椭圆环形桌位）

## 改动前的问题
原座位采用 `players-grid` **平铺网格**（按座位号从左到右、从上到下排列）：
- 完全没有三国杀标志性的「环形桌位」空间感；
- 看不出出牌顺序的流向与上下家关系；
- 人类玩家座位随机，缺少「自己这边」的仪式感；
- 牌堆信息条孤立在顶部，没有「桌心」概念。

## 优化方案
把平铺网格重构为 **椭圆环形桌位（Round Table Layout）**：

1. **椭圆桌位舞台 `.seat-table`**
   - 新增桌面质感：径向渐变台面 + 同心圆纹理（`repeating-radial-gradient`），像一张木质牌桌。
   - 桌心 `.table-center` 居中放置「牌堆 + 出牌顺序 ↺ 流向提示」。

2. **极坐标定位玩家（核心）**
   - 每个 `.player-slot` 用百分比极坐标定位：
     `left: calc(50% + var(--x)); top: calc(50% + var(--y)); transform: translate(-50%,-50%)`
   - 角度由 JS 计算：人类玩家**固定桌底（180°）**，其余玩家按座位号均匀环绕，并整体旋转使人类始终在底部。
   - 半径用**百分比**（`--x` 相对桌宽、`--y` 相对桌高），随容器自适应缩放，桌面/平板都不变形。
   - 全部走 `transform`/`left`/`top`，**纯合成器线程，零主线程重排**——契合本项目 `#app` 每帧 `innerHTML` 重建的约束。

3. **人类专属底部操作托盘 `.human-tray`**
   - 人类手牌与装备从环形上**拆出**，移到桌面底部的独立横条，避免挤在环形槽位导致溢出/重叠。
   - 环形上的人类面板只保留状态信息（头像/血量/技能/装备标识），视觉更清爽。

4. **响应式降级**
   - `@media (max-width: 850px)`：`.seat-table` 退化为 `grid` 平铺（沿用原有网格思路），`table-center` 占满首行，槽位 `position: static`，移动端安全可用。

## 性能与预算
- 定位仅用 `transform`/`left`/`top` + CSS 变量，无逐帧 JS 重排；
- 桌面/移动双达标：宽屏椭圆桌位，窄屏自动网格；
- 未引入新的昂贵模糊或阴影动画，保留此前 VFX 覆盖层的 GPU 合成策略。

## 改动文件
- `game.js`：`render()` 中座位渲染段（约 2566–2709）重构为环形 + 底部托盘。
- `game.css`：新增 `.seat-table` / `.table-center` / `.table-ring-hint` / `.player-slot` 极坐标定位 / `.human-tray`；850px 断点降级；移除原 `.players-grid` 样式。

## 预览
本地静态服务：`http://localhost:8123/index.html`（已就绪）。
开局后可见：玩家环绕牌桌、人类在底部、牌堆居中、桌心显示出牌流向；缩窄窗口至 850px 以下自动变为网格布局。
